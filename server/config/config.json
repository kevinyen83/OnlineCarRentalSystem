{
    "development": {
        "username": "${USER}",
        "password": "${PASSWORD}",
        "database": "${DATABASE}",
        "host": "${HOST}",
        "port": "${PORT}",
        "dialect": "mysql"
    },
    "test": {
        "username": "${USER}",
        "password": "${PASSWORD}",
        "database": "${DATABASE}",
        "host": "${HOST}",
        "port": "${PORT}",
        "dialect": "mysql"
    },
    "production": {
        "username": "${USER}",
        "password": "${PASSWORD}",
        "database": "${DATABASE}",
        "host": "${HOST}",
        "port": "${PORT}",
        "dialect": "mysql"
    }
}